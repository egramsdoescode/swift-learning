// You can access the minimum and maximum values of each integer type using the min and max properties
let minVal = Int32.min
let maxVal = Int32.max

print("Min Int32: \(minVal), Max Int32: \(maxVal)")

// By default, the Int type will be 64 bit on 64 bit platforms and 32 on 32 bit platforms
